"""The interpretability lens: captured conduct calls, read by a local model on request.

Every figure here describes Qwen3-0.6B reading the exact prompt a hosted model was sent,
never the hosted model's internals. How often Qwen picks the same tool is reported beside
every reading, so a reader knows how far to trust it as a stand-in.
"""
