# Continuous archiving, as a file you add rather than a profile you enable.
#
#   docker compose -f docker-compose.prod.yml -f docker-compose.backup.yml up -d
#
# It lives apart from docker-compose.prod.yml for the reason that file gives for
# living apart from docker-compose.yml: separate concerns belong in separate files
# rather than in a pile of overrides. Here there is a second, harder reason.
#
# The five ${BACKUP_*:?} values below refuse to start a backup service that has not
# been told where to write or how to encrypt. That is the right behaviour and it is
# why they are written that way. But Compose interpolates a whole file before it
# selects profiles, so while this service sat in docker-compose.prod.yml behind
# profiles: ["backup"], those five were required to bring up the stack at all,
# backups wanted or not:
#
#   required variable BACKUP_S3_BUCKET is missing a value: set BACKUP_S3_BUCKET
#
# The prod file's own comment claimed the opposite, that a deployment with no
# repository configured "runs exactly as it did". Moving the service here makes that
# true, and keeps the loud failure exactly where it belongs: on the deployment that
# asks for backups without configuring them.
#
# See docs/BACKUP.md, and read its "What is not done yet" section before trusting
# any of this: no bucket exists yet and no restore has been run.

name: elenchus-prod

services:
  # Continuous archiving to object storage. The database ships each write-ahead log
  # segment as it closes, so recovery is to a moment rather than to last night: the
  # difference between losing a shift's answers and losing the ones since 14:32.
  #
  # Empty by default and inert when unset, like every other credential here. A
  # deployment with no repository configured runs exactly as it did, with no backups and
  # nothing pretending otherwise. See docs/BACKUP.md.
  backup:
    image: pgbackrest/pgbackrest:2.55
    environment:
      PGBACKREST_REPO1_TYPE: s3
      PGBACKREST_REPO1_S3_BUCKET: ${BACKUP_S3_BUCKET:?set BACKUP_S3_BUCKET to enable backups}
      PGBACKREST_REPO1_S3_ENDPOINT: ${BACKUP_S3_ENDPOINT:?set BACKUP_S3_ENDPOINT}
      PGBACKREST_REPO1_S3_KEY: ${BACKUP_S3_KEY:?set BACKUP_S3_KEY}
      PGBACKREST_REPO1_S3_KEY_SECRET: ${BACKUP_S3_KEY_SECRET:?set BACKUP_S3_KEY_SECRET}
      PGBACKREST_REPO1_S3_REGION: ${BACKUP_S3_REGION:-auto}
      PGBACKREST_REPO1_PATH: /elenchus
      # Encrypted in the repository, so the bucket holding it is not also a copy of every
      # answer in plain text. The passphrase is not recoverable from the backup: lose it
      # and the backups are noise.
      PGBACKREST_REPO1_CIPHER_TYPE: aes-256-cbc
      PGBACKREST_REPO1_CIPHER_PASS: ${BACKUP_CIPHER_PASS:?set BACKUP_CIPHER_PASS}
      # A guess until the plant's own record-retention standard replaces it: BRCGS ties
      # retention to shelf life plus customer requirements, and this file should not
      # invent that number.
      PGBACKREST_REPO1_RETENTION_FULL: ${BACKUP_RETENTION_FULL:-12}
      PGBACKREST_PG1_HOST: postgres
      PGBACKREST_PG1_PATH: /var/lib/postgresql/data
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
